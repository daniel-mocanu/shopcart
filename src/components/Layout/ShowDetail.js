const ShowDetail = () => {

    return (
            <div style={{color: "white"}} align="center"> 
                <p> Your cart is empty, click "Add to Cart"</p>
            </div>
    );
};

export default ShowDetail;